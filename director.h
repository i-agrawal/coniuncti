#ifndef __director_h__
#define __director_h__

/*
  func direct
    -- purpose: add and delete nodes,
                create proxies from external
                endpoints to local endpoints,
                create proxies from local endpoints
                to external endpoints
*/
void* direct(void*);

#endif
